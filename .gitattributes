# Auto detect text files and perform LF normalization
* text=auto

# JS and CSS files must always use LF
*.js text eol=lf
*.css text eol=lf

# SVG treated as binary
*.svg binary

# Denote all files that are truly binary and should not be modified
*.png binary
*.jpg binary
*.gif binary
*.ico binary